{
  "name": "autumn-recruitment-workbench",
  "private": true,
  "workspaces": ["shared", "client", "server"],
  "scripts": {
    "dev": "npm run dev --workspace=client & npm run dev --workspace=server",
    "build": "npm run build --workspace=shared && npm run build --workspace=client && npm run build --workspace=server",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "npm run typecheck --workspace=shared && npm run typecheck --workspace=client && npm run typecheck --workspace=server",
    "lint": "npm run lint --workspace=shared && npm run lint --workspace=client && npm run lint --workspace=server"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.2.0"
  }
}
